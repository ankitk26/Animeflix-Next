import Layout from "./Layout";

export default function ErrorMessage() {
  return (
    <Layout title="Error! Try again">
      <p>Try refreshing the page or checking your internet connection</p>
    </Layout>
  );
}
